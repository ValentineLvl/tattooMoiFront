import React from 'react'
import { StyleSheet, Text, View, Button, ScrollView, SafeAreaView, TextInput, Image, TouchableOpacity } from 'react-native';

function AppointmentTatoueurScreen() {
    return (
        <View>
            <Text>Mes demandes</Text>
        </View>
    )
}

export default AppointmentTatoueurScreen
